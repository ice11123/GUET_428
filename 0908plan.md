# 0908plan — 实验室分组首页重构

## 目标

- 首页在现有首屏与波浪后依次展示：电源组、飞控组、小车组、其他、维护公告。
- 三个作品组使用透明作品图形成溢出横框的克制 3D 层次；其他组使用纯排版入口。
- 移除首页旧的全站“近期文章”，每组最多展示最近 2 篇文章。
- 将站点统计、技术构成和运行状态迁入关于页。
- 固定四个一级目录并确保空目录也可访问；演示文章迁入“其他”，旧链接保留重定向。
- 最终推送 GitHub Pages 与 Sites，在线验收后删除本文件。

## 已锁定决策

- 一级目录顺序：`电源组 → 飞控组 → 小车组 → 其他`。
- 小车作品图使用 `小车组.png`；额外的 `小车.png` 不采用、不删除、不提交。
- 横框进入现有分组文章页；空分组显示“暂无文章”。
- 演示文章显示在“其他”的近期文章中。
- 维护公告使用最近 3 条可展开列表，默认收起，位于主页最底部。
- 仅使用 CSS `transform`、`opacity` 和阴影微交互；触摸设备不触发 hover，减少动态模式取消位移。
- 本任务不创建 GitHub OAuth 凭据，不启用管理台写入权限，不修改 `blog_test2`。

## 阶段状态

- [x] 阶段 0：核验现有结构、素材与部署配置；创建并认领 Beads `guet428-892`。
- [ ] 阶段 1：建立四组统一模型、内容约束、固定分类路由和目录空状态。
- [ ] 阶段 2：迁移演示文章，接入三张作品图并完成首页横框。
- [ ] 阶段 3：迁移站点信息到关于页，重做底部维护公告。
- [ ] 阶段 4：更新管理台目录选择与自动测试，完成响应式/可访问性检查。
- [ ] 阶段 5：完整测试、类型检查、GitHub/Sites 双构建与在线验收。
- [ ] 阶段 6：删除本文件，提交推送，关闭 Beads 并同步。

## 已知事实与约束

- 选定图片均为带 Alpha 通道的高分辨率 PNG，必须通过 Astro 图片管线生成响应式资源。
- 当前仅有一篇“博客功能演示文档”，其余三个作品组初始为空。
- 当前分类页只从已有文章生成，侧栏也只显示非空目录，需要显式加入固定目录。
- 当前维护记录内容为空，主页必须提供不会破坏布局的空状态。
- GitHub Pages 使用 `/GUET_428` 基路径；Sites 使用 `/` 基路径，必须分别构建验证。

## 错误与验证记录

| 时间 | 阶段 | 结果 |
| --- | --- | --- |
| 2026-09-08 | 初始化 | 等待实施 |

